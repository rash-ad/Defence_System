This project demonstrates a Defense System that includes Submarine, Tank, and Helicopter, all built using Java Interfaces.
Instead of using inheritance, the project uses a common interface so every defense unit follows the same structure but implements its own unique behavior.
